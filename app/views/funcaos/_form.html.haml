= form_for @funcao, :html => { :class => 'form-horizontal' } do |f|
	.control-group
		= f.label 'Descrição', :class => 'control-label'
		.controls
			= f.text_field :descricao, :class => 'span5'
	.form-actions
		= f.submit 'Atualizar', :class => 'btn btn-primary'
		= link_to t('.cancel', :default => t("helpers.links.cancel")), funcaos_path, :class => 'btn'
/= f.actions do
/ = f.action :submit, :as => :input
